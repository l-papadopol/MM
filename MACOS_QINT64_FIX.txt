MadModem 0.5.78 FT8 atomic TX lifecycle fix — macOS Qt6 qint64 portability correction

Corrected two qBound<qint64>() calls in mainwindow.cpp that used bare integer literals.
Qt6/AppleClang may reject those calls as ambiguous.

Changes:
- qBound<qint64>(0, ..., 60000)
  -> qBound<qint64>(qint64{0}, ..., qint64{60000})

Locations in the source revision:
- FT TX arm delay calculation
- millisecondsToNextFt8TxPeriod()

Validation performed:
- scripts/check_macos_portability.sh: PASS

No FT8/FT4 sequencer logic, timing constants, CAT/PTT behavior, decoder code, CW code,
AudioEngine, or waterfall code was changed.
