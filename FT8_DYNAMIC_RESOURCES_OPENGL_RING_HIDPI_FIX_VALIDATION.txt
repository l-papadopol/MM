MadModem 0.5.78 — FT8 dynamic resources / OpenGL ring HiDPI fix

Source basis:
  MadModem_0_5_78_FT8_DYNAMIC_RESOURCES_OPENGL_RING_FULL_SOURCE

Observed evidence:
  - OpenGL circular backend active
  - texture 1024x210
  - render about 0.5-1.5 ms
  - dropped rows 0
  - GL image limited to approximately half width and half height while overlays span full widget

Correction:
  widgets/WaterfallWidget.cpp
  - CPU-fallback GL clear viewport scaled by devicePixelRatioF()
  - circular-texture draw viewport scaled by devicePixelRatioF()

Unchanged:
  FT8/FT4 decoder and thresholds, SystemResourceManager, AudioEngine, native CW, sequencer, CAT/PTT and other modes.

Preparation checks:
  - exact source diff reviewed
  - both glViewport calls audited
  - no direct logical-pixel glViewport call remains
  - full Qt build not performed: Qt development packages are absent in the preparation container

Required live checks:
  1. build on the normal Linux/Windows toolchain
  2. test downward waterfall at 100% and HiDPI scaling
  3. verify full-area fill and overlay/marker alignment
  4. verify runtime log keeps dropped rows at zero under normal load
