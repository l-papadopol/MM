MadModem 0.5.79 — GitHub build fix

Cause confirmed by both macOS ARM64/Qt6 and Linux/Qt5 CI logs:
mainwindow.cpp used rttyContestAdifKey() in addQsoToLogFromForm() before the helper was declared.
The helper definition existed later in the same translation unit, but C++ requires a declaration before use.

Fix:
- moved the single rttyContestAdifKey() helper definition into the existing top-level anonymous namespace near the start of mainwindow.cpp;
- removed the later duplicate definition;
- no RTTY contest logic, ADIF key format, waterfall/DSP code, FT sequencer, CAT behavior, or rules data was changed.

Static verification performed in this environment:
- exactly one rttyContestAdifKey() definition remains;
- the definition precedes all call sites;
- 0.5.79 version identifiers remain unchanged;
- rtty_rules and RTTY_RULES_CATALOG_2026.txt are present in the full-source package.

A full Qt build was not executed in this container because Qt development packages are not installed here.
