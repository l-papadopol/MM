MadModem 0.5.78 — OpenGL ring HiDPI + label fix validation

Source audit:
- Qt5 high-DPI scaling and high-DPI pixmaps enabled before QApplication
- Qt6 relies on built-in high-DPI support
- both OpenGL viewport paths multiply logical size by devicePixelRatioF()
- fractional DPR is rounded to the physical framebuffer dimensions
- QOpenGLWidget uses NoPartialUpdate because the full frame is reconstructed
- GL_SCISSOR_TEST is disabled before full framebuffer clear/draw
- full RGBA color mask restored before clear
- QPainter TextAntialiasing explicitly enabled
- waterfall grid/marker geometry remains pixel aligned

Functional scope:
- no decoder, DSP, audio, sequencer or resource-controller changes
